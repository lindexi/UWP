<UserControl
    x:Class="Button.Control.ProgressButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Button.Control"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Button Margin="10,10,10,10"
                IsEnabled="{x:Bind Enable,Mode=OneWay}"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch"
                Click="ProgressButton_OnClick">
            <Button.Content>
                <Grid>
                    <TextBlock Text="{x:Bind Text,Mode=TwoWay}" ></TextBlock>
                    <ProgressRing Margin="10,10,10,10" IsActive="True" Visibility="{x:Bind ProgressVisibility,Mode=TwoWay}"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"></ProgressRing>
                </Grid>
            </Button.Content>
        </Button>
    </Grid>
</UserControl>
